import React from 'react';

const CTASection = () => {
  // This component is kept for backwards compatibility
  // New in-content CTAs are now used on each page instead
  return null;
};

export default CTASection;
